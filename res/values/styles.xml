<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->

    </style>
    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="circularProgressBarStyle">@style/CircularProgressBar</item>
        <item name="toolbarStyle">@style/MyToolbar</item>
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
        
    </style>

    <style name="MyActionBarStyle" parent="@style/Widget.AppCompat.ActionBar">
        <item name="background">@drawable/action_background</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
        <!-- 
        <item name="background">@drawable/action_background</item>
         -->
    </style>
    
    <style name="cust_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowTitleStyle">@style/dialog_title_style</item>
    </style>

    <style name="dialog_title_style" parent="android:Widget.TextView">
        <item name="android:background">#ffd900</item>
        <item name="android:height">32dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:gravity">center_horizontal|center_vertical</item>
        <item name="android:textColor">#6a3906</item>
    </style>

    <style name="RadioButtonStyle" parent="@android:style/Widget.CompoundButton">
        <item name="android:background">@drawable/useinfo_selector</item>
        <item name="android:button">@null</item>
    </style>

    <style name="RadioButtonStyle2" parent="@android:style/Widget.CompoundButton">
        <item name="android:background">@drawable/useinfo_selector2</item>
        <item name="android:button">@null</item>
    </style>

    <style name="CircleTheme" parent="android:Theme.Holo">
        <item name="circularProgressBarStyle">@style/CircularProgressBar</item>
    </style>

    <!-- navi버튼 왼쪽 공백 제거 -->
    <style name="MyToolbar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="contentInsetStart">0dp</item>
    </style>

  

</resources>

